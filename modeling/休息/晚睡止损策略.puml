@startuml 晚睡止损流程
skinparam DefaultTextAlignment center

title 晚间止损执行流程（反拖延/反放飞）
start

:当前时间接近或超过计划入睡时间;

if (是否出现\n“反正已经晚了”想法?) then (是)
  :触发止损规则;
  :停止当前行为（刷手机/发呆/拖延）;

  if (是否还在做高刺激行为?) then (是)
    :立刻关闭手机/电脑;
  else (否)
  endif

  :进入“最小睡觉版本”;

  partition 最小睡觉版本 {
    :简单洗漱（可跳过复杂步骤）;
    :关闭灯光或降低环境刺激;
    :直接上床;
  }

  if (舍友仍未睡?) then (是)
    :使用耳塞/眼罩;
    :允许环境不完美;
  else (否)
  endif

  :躺下休息(不追求立刻入睡);

else (否)
  :按正常流程准备睡觉;
endif

:记录今日执行(否成功止损);

stop
@enduml