@startuml 起床策略系统
skinparam DefaultTextAlignment center
title 起床行为控制 + 运动执行系统(6:30起床)

start

:时间 = 6:30;
:闹钟响;

if (是否成功起床?) then (是)

    :离开床;

    :进入晨间运动阶段;
    :跑步;
    :跳绳;
    :基础锻炼;

    :吃早饭;

    :进入学习/上课准备;

    stop

else (否)

    :起床失败;

    :分析原因;

    if (是否因为前一晚晚睡?) then (是)

        :记录晚睡原因;
        :分析问题（如拖延/手机等）;

        :触发惩罚机制;
        note right
            其实也没什么惩罚,
            还是心不诚, 具体
            请看./贪心与动态规划/
            动态规划.puml,
            贪心.puml
        end note

    else (否)

        if (是否为运动阻力过大?) then (是)

            :降低运动强度;
            note right
                最小执行策略：
                - 跑一小圈
                - 跳绳100下
                - 或者更小阻力
            end note

            :完成最低运动任务;
            :吃早饭;
            :进入学习状态;

        else (否)

            :判定为“纯粹不想起床”;

            :触发惩罚机制;
            note right
                其实也没什么惩罚,
                还是心不诚, 具体
                请看./贪心与动态规划/
                动态规划.puml,
                贪心.puml
            end note

        endif

    endif

endif

stop
@enduml