<%*
const types = ["一级页表", "二级页表"];
const type = await tp.system.suggester(types, types);

const tasks = ["主任务", "子任务"];
const task = await tp.system.suggester(tasks, tasks);

const subject = await tp.system.prompt("学科");

const title = await tp.system.prompt("任务名称");
const id = tp.date.now("YYYYMMDDHHmm");

const startMoment = moment(); 
const plan_start = startMoment.format("HH:mm"); 
let plan_end; 
if (type === "一级页表") {
	plan_end = startMoment.add(50, 'minutes').format("HH:mm"); 
} else {
	plan_end = startMoment.add(25, 'minutes').format("HH:mm"); 
}

tR += `---
id: ${id}
task: ${task}
subject: ${subject}
task_name: ${title}
duration: ${plan_start}-${plan_end}
actual_duration: "" 
performance: "效率高 良好 效率差 打断多 玩手机 走神 干其他事"
tags: []
---

# 任务描述
# 执行评价
`;

await tp.file.rename(`${tp.date.now("YYYYMMDDHHmm")}-${title}`);
%>
