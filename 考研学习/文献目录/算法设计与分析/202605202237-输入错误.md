---
id: 202605202237
type: 方法
subject: 算法
source: ""
page: ""
islinked: false
tags: []
---

|组成|描述|关联|
|:--:|:--:|:--:|
# 描述
- 输入错误并不是宏观的输入数据时的错误, 而是**编写算法前出现错误的汇总**
- 编写算法前,
	- 需要**检查数据是否完整输入**
	- 有无内存错误
# 组成
 - [[202605202207-内存错误|内存错误]]
	 - 一般这是开辟数据结构时所犯的错误
	 - 数据输入不齐全
~~~C
static void _init(IO_ *io) {
  io->array = (int *)malloc(sizeof(int) * array_size(io));
  io->query = (int *)malloc(sizeof(int) * query_size(io));
  io->output = NULL;
  io->output_count = 0;
}

void input(IO_ *io) {
  scanf("%d %d", &io->n, &io->m);
  _init(io);
}
//开辟array, query后未输入数据
~~~
- [[202605211153-算法参考系|[[算法参考系]]]]
	- $for$循环中完全依照base-0思想